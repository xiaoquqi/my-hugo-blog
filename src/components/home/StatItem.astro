---
interface Props {
  number: string;
  label: string;
}

const { number, label } = Astro.props;
---

<div class="home-stat-item">
  <span class="home-stat-number">{number}</span>
  <span class="home-stat-label">{label}</span>
</div>
